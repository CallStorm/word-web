# Phase 1 Web MVP 依赖
# 安装：.venv/bin/pip install -r backend/requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.0
aiofiles>=24.0

# Phase 2: auth + multipart upload
passlib[bcrypt]==1.7.4
bcrypt<4.2  # passlib 1.7.4 跟 bcrypt 5+ 不兼容（detect_wrap_bug 触发 72-byte 限制）
python-jose[cryptography]==3.3.0
python-multipart==0.0.12

# DB driver: MySQL（需先 docker 启动 MySQL，见 README）
# DB_URL=mysql+pymysql://pptweb:pptweb@127.0.0.1:3306/pptweb?charset=utf8mb4
pymysql>=1.1

# Phase 3: 文档文本提取（智能填充文档模式，复用 ppt-master source_to_md 同款库）
# 仅在 backend 进程内把上传文档转成 markdown 文本喂给 LLM，不写盘、不做图片处理
PyMuPDF>=1.23.0          # PDF
mammoth>=1.6.0           # DOCX
python-pptx>=0.6.21      # PPTX（pin <0.7，见 ppt-master 说明）
openpyxl>=3.1.0          # XLSX/XLSM
markdownify>=0.11.6      # HTML → markdown
beautifulsoup4>=4.12.0   # HTML 清洗
